import React from 'react';

const Dropdown = ({ item, idx, dropDowns }) => {
    return (
        <div key={idx}>
            <input type="radio" name="" id="" />  <input className='dropdowns' type="text" placeholder="" />
        </div>
    );
};

export default Dropdown;